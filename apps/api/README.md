# API workspace

The backend API will be implemented in Phase 2. It will expose authenticated, versioned boundaries for documents, conversations, generation, quizzes, progress, study plans, agent operations, evaluations, and health.

Do not add runtime API behavior in the foundation phase.
